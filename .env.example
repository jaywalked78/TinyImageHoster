# Server configuration
IMAGE_SERVER_PORT=7779
IMAGE_SERVER_HOST=0.0.0.0

# Timeout settings (in minutes, 0 = disabled)
# After this time, images will be automatically unloaded
IMAGE_SERVER_TIMEOUT=30

# Processing Configuration
# Base directory for screen recordings (used for relative path resolution)
FRAME_BASE_DIR=/path/to/your/images/directory 